<?xml version="1.0"?>
<thlogic>
    <attr sel="#header" th:replace="header :: header" />
    <attr sel="#footer" th:replace="footer :: footer" />
            <!--  이걸 이런식으로 th:object 해서 attribute 를 얻어오게 되면  -->
   <arrt sel="#article-main" th:object="${article}">
<!--            원래 th:text="${article.title} 로 해야되는데 이런 표현이 가능해짐"-->
<!--        <arrt sel="#article-header/h1" th:text="${article.title}" />-->
<!--        <arrt sel="#nickname" th:text="*{nickname}" />-->
<!--        <arrt sel="#email" th:text="*{email}" />-->
<!--        <arrt sel="#created-at" th:datetime="*{createdAt}" th:text="*{#temporals.format(createdAt,'yyyy-MM-dd HH:mm:ss')}" />-->
<!--        <arrt sel="#hashtag" th:text="*{hashtag}" />-->
<!--        <arrt sel="#article-content/pre" th:text="*{content}" />-->
    </arrt>
</thlogic>